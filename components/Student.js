/* eslint-disable no-unused-vars */
import Score from "./Score";

function Student({ name, bio }) {
  return (
    <>
      <h1>{name}</h1>
      <p>{bio}</p>
      <p></p>
    </>
  );
}

export default Student;
